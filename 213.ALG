Algoritmo "semnome"

Var

   X, Y, Z, A, B, C, D : real


Inicio

   X <- 3
   Y <- 9
   Z <- -13
   
   A <- EXP(X, Y)
   B <- RAIZQ(Y)
   C <- PI
   
   escreval(a)
   escreval(b)
   escreval(c)


Fimalgoritmo